line-length = 120
target-version = "py311"

[lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM", "RUF"]
ignore = ["RUF001"]  # the pipeline intentionally uses non-ASCII symbols (emoji legends) in rendered Markdown
